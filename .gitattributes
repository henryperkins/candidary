worker-configuration.d.ts text eol=lf
migrations/*.sql text eol=lf
config/*.json text eol=lf
config/*.json.sha256 text eol=lf
# Upstream license copies are SHA-256 pinned byte for byte; never normalize their line endings.
tests/fixtures/mobile-images/licenses/* -text
# Decoder build-fingerprint inputs are hashed as raw bytes (scripts/lock-image-decoder.mjs); never normalize them.
services/image-decoder/native/** -text
shared/image-decoder-contract.ts -text
shared/image-formats.ts -text
# Qualification records pin the corpus manifest by SHA-256; keep its bytes identical on every checkout.
tests/fixtures/mobile-images/manifest.json -text
