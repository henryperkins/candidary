# Host Gallery Roadmap — Verification Matrix

**Program:** [Host Gallery Roadmap](specs/2026-08-23-host-gallery-roadmap-program-design.md)

Every finding from the [Combined Host Gallery Experience Review](../../gallery-host-combined-review.md)
appears here exactly once, with one of four dispositions:

- `verified-existing` — current code already satisfies the finding, and a regression proves it;
- `implemented` — this roadmap changed code and the regression passes;
- `deferred-approved` — cross-audience withdrawal only;
- `out-of-scope-approved` — broader ownerless recovery only.

A row is complete only when its owning test file names a real test that fails without the change.
Slices 2–6 fill their own sections as they land; this file is the program's single record.

There is no partial disposition, and none may be added. A finding whose work spans several checkpoints
gets **one row, written by the checkpoint that closes it**, naming every checkpoint's evidence
together. An earlier checkpoint that proved only part of a finding records that as prose above its
section's table — plainly labelled as progress — and writes no row. A row that exists is a claim that
the finding is settled, and every one of the four dispositions says so.

## Final commands

```bash
npm run typecheck
npm run typecheck:e2e
npm run lint
npm test
npm run build
npm run ci:migrations
npm run test:e2e
```

## Slice 1 — Privacy, recovery, and Manager isolation

| Finding | Disposition | What changed | Owning tests |
| --- | --- | --- | --- |
| **C-03** — guest captions reach Album link holders with no moderation model applied or disclosed | `implemented` | One public Album projection (`worker/services/public-album.ts`) serves both the album-share read and Manager Preview, and selects `CASE WHEN publication_status = 'published' THEN caption ELSE NULL END`. Publication controls caption eligibility for the Album link only; membership is unchanged in all six cases. | `tests/worker/album-share-api.test.ts`, `tests/worker/album-api.test.ts` |
| **C-04** — Intake permanently deletes an original in one tap, with no confirmation, feedback, focus, or recovery | `implemented` | The generic media PATCH lost `delete`. `POST .../media/:mediaId/trash` is recoverable and confirmed; migration `0019_media_recovery.sql` adds the trash pair, the recoverable counters, and the capacity guard; `MediaRepository.trashStored` / `restoreTrashed` / `permanentlyDeleteTrashed` own the transitions; the Manager adds a focused confirmation, deterministic post-action focus, an announcement carrying the server deadline, a 30-second Undo capped by `restoreUntil`, and a Recently deleted filter. | `tests/ui/manager-recovery.test.tsx`, `tests/worker/media-recovery-api.test.ts`, `tests/worker/migration-0019.test.ts`, `tests/worker/manage-api.test.ts` |
| **C-06** — the guest gallery endpoint serializes internal media fields | `implemented` | `GuestGalleryMediaView`, `GuestContributionMediaView`, `UploadMediaView`, `UploadBatchItemView`, and `GuestContributionDeletionView` are explicit allowlists in `shared/contracts.ts`; every guest response is mapped through them and sends `private, no-store` plus `Vary: Cookie`. The shared gallery says `Shared photo` rather than exposing a filename. | `tests/worker/upload-api.test.ts`, `tests/ui/guest-upload-flow.test.tsx` |
| **C-11** — Stop sharing revokes a distributed URL with no warning | `implemented` | A focused confirmation states immediate and irreversible URL/session revocation, the already-loaded-copy boundary, and what is unchanged; initial focus is **Keep sharing**, Escape and cancel send no request, **Stop Album link** is a nondefault `type="button"`, and success announces revocation and moves focus to the replacement action or the sharing heading. | `tests/ui/album-workspace.test.tsx` |
| **C-22** — Intake's contributor filter silently constrains another destination | `implemented` | Intake owns a discriminated `active` / `trash` query. The mode leads the query identity; the contributor filter and publication status belong to `active` alone and are absent from the trash request, and neither mode's URL can carry the other's cursor. | `tests/ui/manager-recovery.test.tsx` |
| **C-35** — one failed read in the Manager's initial `Promise.all` replaces every panel with an error page | `implemented` | `src/features/manager/resources.ts` gives every Manager resource its own controller with `{ eventId, queryKey, generation, value, status, failure }`. The event is the only shell-critical read; a retryable failure stays in its panel and keeps the last trusted value across Retry; a credential, role, account, or lifecycle failure from any resource escalates once to the recovery surface; a retired generation's response is dropped. | `tests/ui/manager-recovery.test.tsx` |
| **export/delete retention race** — an accepted export can lose a source object a host later deletes | `implemented` | `export_media_entries` plus a `queued`/`running` owner is a D1 source hold. Migration 0019 adds `export_media_entries_source_hold` and triggers that refuse suppression of a held key, refuse freezing an already-suppressed source, fence `queued → running` on the frozen proof, and fence `terminal → queued` retry on every frozen entry still resolving to an active-or-recoverable stored row at that exact key. Creation ends in a set-equality sentinel; retry reports `EXPORT_SOURCE_REMOVED`; `claimMediaObjectDeletion` is the only way to win the right to delete bytes. | `tests/worker/export-api.test.ts`, `tests/worker/migration-0019.test.ts`, `tests/worker/media-recovery-api.test.ts` |

### Named expansions

The spec forbids broad phrases standing in for coverage. The two that slice 1 uses expand to these
exact lists.

**Every ordinary media query that must exclude a trashed row.** Each has a fixture proving the row
would otherwise appear:

| Surface | Query |
| --- | --- |
| Manager Intake (active) | `buildManagerMediaQuery` |
| Library / private Gallery | `MediaRepository.listGalleryTimeline` (including its search branch) |
| Album picks | `albumPickQuery` (retained picks resolve to opaque markers, never to photos) |
| Public and Preview Album | `PublicAlbumService.project` |
| Guest gallery | `MediaRepository.listGallery` |
| Guest's own contributions | `MediaRepository.listContributions` |
| Guestbook caption feed, Manager projection, caption item, export snapshot | `worker/db/guestbook.ts` (four sites) |
| Preview and original delivery | `worker/routes/content.ts` (two sites) |
| Export creation, both kinds | `eligibleSourceSql` in `worker/db/exports.ts` |
| Publication writes | `setPublication`, `setPublicationBulk` |
| Favorite writes and album capacity | `setFavorite`, `setFavoriteBulk`, `addFavoritesWithinAlbumCapacity` |
| Timeline maintenance | `countStoredTimelineSentinels`, `repairStoredTimelineSentinels` |
| Legacy preview pointer | `setPreviewObjectKey` |

The only paths permitted to read a trashed row are `listTrashed`, `listExpiredTrashed`,
`restoreTrashed`, `permanentlyDeleteTrashed`, the guest self-delete transition, the purge
terminalization, and the Manager Album retained-slot resolver.

**Every destructive action that must be confirmed before a request is sent.** Slice 1 adds two, and
each is asserted by "no request precedes the confirmation", initial focus on the nondestructive
control, Escape and cancel sending nothing, and exactly one request on explicit activation:

| Action | Surface |
| --- | --- |
| Move a photo to Recently deleted | Manager Intake |
| Stop the Album link | Manager Album sharing |

## Slice 2 — Audience clarity and Album-link creation

| Finding | Disposition | What changed | Owning tests |
| --- | --- | --- | --- |
| **C-01** — live Album membership changes were invisible at the Library action point | `implemented` | Library and Album persistently show the exact live consequence: later saved membership, metadata, sections, and order changes affect what people with the Album link see when they request it. | `tests/ui/album-workspace.test.tsx` (`keeps the exact live Album-link consequence visible in Library and Album`) |
| **C-02** — Hide appeared to withdraw an Album-link photo even though it only changes Guest-gallery publication | `implemented` | Action-point and link copy name the two independent audiences: Hide/Publish affect event guests in the Guest gallery, while Album membership and the Album link remain separate. The whole-product cross-audience withdrawal action remains the approved deferred non-goal; Hide does not withdraw Album-link access. | `tests/ui/album-workspace.test.tsx` (`keeps publication and Album membership independent and announces reversible single results`) |
| **C-18** — publication actions lacked named, counted, audience-specific confirmation | `implemented` | Single and bulk publication announcements identify the photo/count, resulting Guest-gallery state, event-guest consequence, and reverse recovery action. | `tests/ui/album-workspace.test.tsx` (`keeps publication and Album membership independent and announces reversible single results`, `announces exact bulk results and the Guest-gallery-off consequence`) |
| **C-20** — empty Album sections appeared publicly and new sections always appended | `implemented` | Empty sections stay editable with an omission warning but are excluded from the shared public projection; new sections insert after the current editor context and retain that order through save/conflict replay. | `tests/ui/album-workspace.test.tsx` (`uses the focused Album entry as the new section insertion anchor`, `flags every empty section while only live photos make a section nonempty`), `tests/ui/album-share-page.test.tsx` (`omits leading, adjacent, and trailing empty section headings in both variants`) |
| **C-24** — Manager Preview diverged from the public Album | `implemented` | Preview and the live page render one `PublicAlbum` content tree with semantic page/embedded variants, matching cover, count, order, captions, empty state, and fallbacks. | `tests/ui/album-share-page.test.tsx` (`keeps cover, copy, count, photo order, captions, and fallback labels identical`), `tests/e2e/accessibility.spec.ts` (`manager Album Preview and the public Album keep their heading hierarchy axe-clean`) |
| **C-28** — delivered photos used conflicting names across Manager surfaces | `implemented` | Manager uses Photo delivery for the intake process, Delivered photos for the retained collection, and Pick / In Album / Remove from Album for membership. Historical favorite/heart wording remains only in internal compatibility identifiers. | `tests/ui/manager-photo-intake.test.tsx` (`uses Photo delivery and Delivered photos as a scheduled event opens`), `tests/ui/host-private-gallery.test.tsx` (`announces the exact In Album result and patches a picked tile without a refetch`, `uses Delivered photos and In Album vocabulary for empty Library states`, `does not describe an unfiltered empty shared list as unpublished`), `tests/ui/album-workspace.test.tsx` (`uses literal Pick and Remove from Album actions with canonical bulk results`, `uses canonical Album-membership result copy and reverses only what changed`, `uses only Album-pick terminology in the exact one-time reconciliation state`) |
| **C-29** — Shared/Share/Published conflated two audiences and hid their state | `implemented` | The third mode is Guest gallery; the persistent summary reports Album photo count, Album-link Off/Live state, and Guest-gallery On/Off plus published count; sharing actions, preview audience label, and status/stop failures say Album link. | `tests/ui/album-workspace.test.tsx` (`renders the persistent audience summary %#`, `offers Library, Album and Guest gallery, and names each audience boundary`, `uses Album link terminology when status loading has no error detail`, `uses the Album link fallback and keeps the host in the dialog when revocation fails`), `tests/ui/album-share-page.test.tsx` (`reads the manager projection and never the recipient credential routes`), `tests/e2e/album-share.spec.ts` (`a manager shares, copies, opens, and stops the same fragment album link`) |
| **C-30** — Guest-gallery cards gave Publish and Hide equal weight | `implemented` | The filter-appropriate transition is primary and the alternate transition is secondary. | `tests/ui/album-workspace.test.tsx` (`keeps publication and Album membership independent and announces reversible single results`) |
| **C-31** — Album credentials remained visible as raw text | `implemented` | Album links use the existing sensitive `CopyableLinkCard`: a fixed mask is shown by default, the full URL is absent from the hidden DOM, and Reveal mounts one read-only field. | `tests/ui/copyable-link-card.test.tsx` (`keeps a %s sensitive link out of hidden DOM, attributes, names, inputs, and focusables`), `tests/ui/album-workspace.test.tsx` (`creates one sensitive Album link after draft settlement and focuses its Copy action`) |
| **C-39** — Library cards omitted Guest-gallery publication state | `implemented` | Every Library card carries one compact Guest-gallery Published/Unpublished/Hidden state while its Album-membership control remains independent. | `tests/ui/host-private-gallery.test.tsx` (`shows one Album membership state and one noninteractive Guest-gallery state per Library card`) |
| **C-40** — selection copy overstated scope and hid the 50-photo cap | `implemented` | Both selection surfaces say exactly how many loaded photos Select all covers and show `N of 50 selected`, derived from the shared selection limit. | `tests/unit/gallery-selection-state.test.ts` (`reports the shared selection count at %i selections`, `truncates additions at the cap and derives all capacity copy from the shared limit`), `tests/ui/host-private-gallery.test.tsx` (`names Select all from the complete loaded photo count`) |
| **C-48** — Guest gallery claimed photos were visible while the surface was off | `implemented` | On-state copy says published photos are visible to event guests; off-state copy says publication choices are saved but the Guest gallery is off. | `tests/ui/album-workspace.test.tsx` (`announces exact bulk results and the Guest-gallery-off consequence`) |
| **C-51** — Guestbook classified suppressed published captions as Hidden | `implemented` | Guestbook calls them `Not currently visible to event guests` and explains that the Guest gallery is off. | `tests/ui/manager-guestbook.test.tsx` (`explains Guest-gallery-off captions, opens Settings, and labels suppressed published captions exactly`) |
| **C-60** — picked Library cards duplicated Album-membership state | `implemented` | Each picked card retains one `In Album` state and one literal Remove action; the duplicate badge is gone. | `tests/ui/album-workspace.test.tsx` (`names pick and selection state independently without a duplicate Album badge`) |
| **C-65** — Remove selected used a completion checkmark | `verified-existing` | The shared selection tray retains the Minus glyph and literal Remove from Album accessible label. | `tests/ui/album-workspace.test.tsx` (`uses literal Pick and Remove from Album actions with canonical bulk results`) |
| **C-66** — Clipboard fallback instructed selection without selecting the URL | `implemented` | Sensitive and non-sensitive fallback values regain focus and complete selection on every failed Copy attempt, including repeated Clipboard absence or rejection. | `tests/ui/copyable-link-card.test.tsx` (`%s Clipboard refocuses and selects the complete sensitive fallback on every Copy attempt`, `%s Clipboard refocuses and selects the complete Event-link fallback on every Copy attempt`) |

## Slice 3 — Completion and liveness

| Finding | Disposition | What changed | Owning tests |
| --- | --- | --- | --- |
| **C-05** — terminal exports permanently displaced preparation of the current source | `implemented` | Terminal Complete and Album cards remain dated frozen snapshots. Ready retains Download; ordinary Failed/Expired retains `Retry this prepared export` against the immutable snapshot; source-removed snapshots omit Retry; and a separate Prepare-current action remains available whenever neither kind is active. | `tests/unit/guestbook-export.test.ts` (`renders the exact complete terminal action matrix for %s`, `renders the exact Album terminal action matrix for %s`, `omits Retry for source-removed %s exports`), `tests/worker/export-api.test.ts` (`retries a terminal-before-claim initial job without duplicating its frozen snapshot`, `reconciles an empty initial createBatch result with its retained %s Workflow`, `reconciles an empty retry createBatch result with its retained %s Workflow`, `keeps an empty retry createBatch result retryable when retained status is unknown`) |
| **C-07** — StrictMode replay could dispose the live Settings or Appearance autosave queue while showing Saved | `implemented` | Each committed editor generation owns one existing autosave queue. StrictMode replay leaves one usable live queue, Saved follows active-response adoption only, and a retired generation cannot update the parent or restore stale UI. | `tests/ui/event-settings-editor.test.tsx` (`keeps one live Settings queue through StrictMode replay and reports Saved only after adoption`, `does not adopt a Settings response after its autosave generation retires`), `tests/ui/event-appearance-editor.test.tsx` (`keeps one live Appearance queue through StrictMode replay and reports Saved only after adoption`, `does not adopt an Appearance response after its autosave generation retires`) |
| **C-13** — a second blocked destination could be stranded behind the first Album preparation | `verified-existing` | The existing destination-keyed Router preparation generation ignores settlement belonging to an earlier destination and proceeds only to the latest blocked destination. The authoritative regression is unchanged from `HEAD`. | `tests/ui/app.test.tsx` (`restarts Album preparation when a blocked router destination changes`) |
| **C-14** — failed or invalid Album saves could indefinitely prevent leaving | `implemented` | Album leave preparation returns explicit invalid or failed outcomes and exposes Retry, Stay, and exact-destination discard consistently across Gallery-mode, Manager-section, and Router exits. Initial settlement waiting offers no inert Retry; an explicit retry retains one focused, busy, deduplicated control. Discard retires only unsent work; an already-sent request may still finish. | `tests/ui/album-workspace.test.tsx` (`returns exact invalid and failed Album leave outcomes`, `discards scheduled Album work while allowing an already-sent request to finish`, `offers Retry, Stay, and exact-destination discard for an invalid Album mode exit`, `automatically commits only the newest Gallery-mode destination after Album settles`, `retries the existing failed Album draft and keeps Retry stable until it settles`), `tests/ui/app.test.tsx` (`offers the Album recovery actions for an invalid manager-section destination`, `offers recovery and discards only to the exact current Router destination after an Album conflict`) |
| **C-19** — equivalent removals had inconsistent Undo behavior and navigation could discard the offer | `implemented` | One Manager-scoped Undo controller owns mount-independent API inverses. Single and bulk Album removals survive Gallery or Album unmounts, and nested interaction holds resume only the exact remaining Undo duration. | `tests/ui/host-private-gallery.test.tsx` (`keeps one persistent Undo bar and retries the exact API-only inverse after Gallery unmounts`, `restores an exact bulk removal through the persistent API-only inverse after Gallery unmounts`), `tests/ui/album-workspace.test.tsx` (`keeps a confirmed photo inverse available after Album unmounts`), `tests/ui/manager-undo.test.tsx` (`resumes the exact remaining duration only after the final nested hold releases`) |
| **C-25** — four Cover styles could initially show Preview not ready without useful loading or retry behavior | `implemented` | Prefetch starts only after the fixed five-style step is visible and remains bounded and deduplicated inside the existing session owner. All radios remain usable; loading/error retains the last usable tile; Retry is selection-neutral, focus-stable, live-announced, and deduplicated through failure; success announces readiness and restores focus to the connected style radio. A failed preview can refetch after reopen without refetching cached effects. | `tests/ui/cover-studio.test.tsx` (`announces Style visibility only after the fixed five choices are on screen`, `keeps style radios usable while real thumbnails load or fail`, `retries a failed thumbnail without changing the selected style`, `keeps a failed preview Retry focused, announced, and deduplicated through another failure`, `announces a successful preview Retry and moves focus to its connected style radio`), `tests/ui/cover-studio-session.test.tsx` (`prefetches all five real effects through one deduplicated session owner`, `retains the last usable tile until an exact replacement succeeds`, `retries a failed effect after close and reopen without refetching cached effects`) |
| **C-26** — offline Album save exposed raw network errors, competing notices, and no reconnect retry | `implemented` | Album offline failure now has one normalized message and one Retry, with no raw network or competing alert. One mounted layout-phase listener catches reconnect before failed-state passive effects, resubmits only the newest valid failed draft, and fires once. | `tests/ui/album-workspace.test.tsx` (`normalizes an offline Album save to one Retry without a competing notice`, `retries only the newest failed Album draft once on reconnect`, `catches reconnect before failed-save passive effects and retries the newest draft once`) |
| **C-32** — retained export error codes were omitted from the Manager view | `implemented` | Manager export projection exposes only the shared safe-code allowlist and maps every unknown internal value to `EXPORT_FAILED`. Presentation maps each safe code to action-oriented host copy, hides raw internal values, and retains partial progress. | `tests/worker/export-api.test.ts` (`projects stored export error %s as safe code %s`), `tests/unit/guestbook-export.test.ts` (`maps %s to safe recovery copy`, `uses generic safe recovery copy for an unknown failure while retaining partial progress`) |
| **C-33** — queued and running exports were indistinguishable and polling stopped outside Gallery | `implemented` | Queued and Running have distinct states; Running shows coarse elapsed time and available progress, and failed work retains partial progress. One Manager-scoped poll and live owner continues outside Gallery, exposes compact status, and stops when the job becomes terminal. | `tests/unit/guestbook-export.test.ts` (`distinguishes the %s state and retains progress detail`), `tests/ui/app.test.tsx` (`keeps one export poll and live owner running outside Gallery until the job is terminal`), `tests/e2e/manager-responsive.spec.ts` (`active export progress stays reachable and contained outside Gallery on narrow screens`), `tests/e2e/accessibility.spec.ts` (`narrow Manager export progress has one scoped live owner and remains axe-clean`) |
| **C-36** — Undo was late, difficult to reach before expiry, and inconsistently announced | `implemented` | One persistent Manager Undo bar survives section changes, keyboard activation can focus Undo while pointer activation preserves the established fallback, repeated identical offers create a fresh live-region mutation, and responsive separation keeps Undo from covering the selection tray. | `tests/ui/manager-recovery.test.tsx` (`keeps one Manager Undo bar and its API-only restore offer across an Intake unmount`), `tests/ui/manager-undo.test.tsx` (`re-adds identical pointer copy to the persistent live region for a replacement offer`, `focuses keyboard Undo, preserves pointer focus, and returns to a connected fallback`), `tests/e2e/manager-responsive.spec.ts` (`the mobile Library tray, reopened Undo, Album, and Guest gallery stay reachable and contained`) |
| **C-41** — one active export kind silently disabled the other kind’s valid controls | `implemented` | The inactive control names whether the Complete collection or Album export is active, identifies its Queued/Running state, and explains that Prepare and Retry return when it finishes. Ready downloads remain usable, and terminalization unlocks the other kind. | `tests/unit/guestbook-export.test.ts` (`names the other active kind, blocks only Prepare and Retry, and keeps a ready download usable`, `names a complete export while Album actions wait`), `tests/ui/album-workspace.test.tsx` (`disables complete prepare and retry for an active album while keeping ready downloads usable`) |
| **C-42** — Reset disclosed section deletion only after activation | `implemented` | Reset’s action-associated copy states beforehand that every section will be removed and derives the nine-second recovery window from the shared Undo duration. The inverse is offered only after the exact reset save and restores the complete prior draft. | `tests/ui/album-workspace.test.tsx` (`explains Reset breadth and derives its Undo duration from the shared window`, `offers Reset Undo only after the exact reset save, then keeps it running through restoration`) |
| **C-47** — live, lifted, and frozen export counts could disagree without freshness or snapshot context | `implemented` | Terminal cards label the frozen `snapshotAt` and photo count separately from the current source. Fresh authoritative counts may show numeric deltas; retained active reads say refreshing, while retained terminal failures say last-known and unavailable without claiming equality or a precise delta. Album count remains owned by the Manager audience authority, and Prepare current stays separate from the frozen card. | `tests/worker/export-api.test.ts` (`projects only the deterministic latest job per kind with normalized Manager timestamps`), `tests/unit/guestbook-export.test.ts` (`describes live source authority without claiming snapshot identity: %o`), `tests/ui/host-private-gallery.test.tsx` (`keeps the ready frozen export and a separate current-source action`), `tests/ui/album-workspace.test.tsx` (`keeps export count on the audience summary while Album reload adopts a concurrent repick`, `keeps the last trusted summary visible when an invalidation retry fails`) |
| **C-54** — Download all was enabled with zero photos | `implemented` | A fresh authoritative zero disables current Complete and Album preparation with a local reason. Notes alone cannot create a new Complete export, while valid historical frozen zero-photo exports remain available. | `tests/ui/host-private-gallery.test.tsx` (`uses a fresh authoritative zero to disable complete preparation with local guidance`), `tests/ui/album-workspace.test.tsx` (`uses the audience summary rather than loaded Album rows to guard an empty export`), `tests/worker/export-api.test.ts` (`rejects a new notes-only complete export without creating a job or dispatching a Workflow`, `keeps a valid historical Ready zero-photo complete export downloadable`) |

## Slice 4 — Canonical Manager URL ownership

| Finding | Disposition | What changed | Owning tests |
| --- | --- | --- | --- |
| **C-15** — Manager section and Gallery mode were not owned by a canonical, traversable URL | `implemented` | `manager-location.ts` is the single parsing, serialization, and href contract. React Router location now directly controls every rendered Manager section and Gallery mode; malformed inputs are canonically replaced without dropping pathname, hash, or Router state; user destinations push history only after Album settlement; Back/Forward uses the same blocker generation; and both Manager recovery surfaces preserve the canonical destination through sign-in. | `tests/unit/manager-location.test.ts`, `tests/unit/recovery.test.ts`, `tests/ui/app.test.tsx` (`canonical Manager location ownership`), `tests/ui/manager-recovery.test.tsx` (`preserves the Manager destination in recovery from canonical Album`), `tests/e2e/album-workspace.visual.spec.ts` (`album mode is keyboard-operable and respects reduced motion`) |
| **C-21** — Cover Studio lets Tab leave the modal | `implemented` | Cover Studio is an `aria-modal` dialog that captures the exact invoker, inerts only the page outside its portal host, locks and restores body scrolling, wraps Tab and Shift+Tab within the sheet, and restores focus to that captured invoker on close. | `tests/ui/cover-studio.test.tsx` (`inerts and scroll-locks the page behind it, and restores both on close`, `restores focus to what opened it`, `keeps focus inside the sheet`) |
| **C-23** — keyboard reordering could move focus to the opposite arrow and gave inadequate positional feedback | `implemented` | Album reorder retains focus on the exact invoked Earlier/Later control after the move, keeps the now-boundary control focusable with `aria-disabled` and a guarded no-op, and announces the moved item name plus `position N of M`. | `tests/ui/album-workspace.test.tsx` (`moves entries with earlier/later controls and equivalent native drag/drop`, `keeps the invoked reorder direction focused and announces item plus position`), `tests/e2e/album-workspace.visual.spec.ts` (`album mode is keyboard-operable and respects reduced motion`) |
| **C-27** — on a 390×844 phone, Gallery chrome pushed the first Library photo below the initial viewport | `implemented` | Mobile Gallery keeps one compact directly reachable control row with native disclosures for secondary context and export detail. At 390 the row is sticky below Manager navigation and leaves at least 44px of the first photo visible below actual obstructions; Library, Album, Guest gallery, audience, search, order, selection, export actions, and the first photo remain reachable. A failed audience read stays in normal flow, fills without escaping its immediate Gallery parent at both 390 and 320, and preserves the same sticky obstruction and native focus-clearance contracts. | `tests/e2e/manager-responsive.spec.ts` (`Library first photo intersects the initial 390 by 844 viewport`, `audience failure stays below the mobile Gallery sticky row`, `the mobile Library tray, reopened Undo, Album, and Guest gallery stay reachable and contained`), `tests/e2e/album-workspace.visual.spec.ts` (`captures and audits the mobile manager states at 390 by 844`) |
| **C-37** — Axe coverage omits Album, Shared, selection mode, the tray, and Album sharing | `implemented` | Exact named readiness gates cover 33 Manager and transient states, two paused-guest route states, two Host-account states, and two public Album states before each full-document Axe scan with the default rules and explicitly enabled `target-size`; one executable assertion locks all inventories in exact order and rejects duplicates both within and across them. Axe `target-size` is the 24px rule and does not replace the separate 44px geometry, responsive/reflow, modal/focus-containment, or keyboard traces. Physical iPhone/Safari, Android/Chrome, VoiceOver, and TalkBack remain `NEEDS RUN`. | `tests/e2e/accessibility.spec.ts` (`Intake default is axe-clean`, `Intake filtered is axe-clean`, `Intake Recently deleted is axe-clean`, `true-empty Intake is axe-clean`, `Manager upload held transfer is axe-clean`, `Cover upload progress is axe-clean`, `RSVP is axe-clean`, `Library default is axe-clean`, `Library selection is axe-clean`, `Library selection tray is axe-clean`, `Library viewer is axe-clean`, `Album editor is axe-clean`, `Album Preview is axe-clean`, `Album create-link dialog is axe-clean`, `Album live-link state is axe-clean`, `Album stop-link alertdialog is axe-clean`, `Guest gallery all is axe-clean`, `Guest gallery unpublished is axe-clean`, `Guest gallery published is axe-clean`, `Guest gallery hidden is axe-clean`, `Guest gallery single-write is axe-clean`, `Guest gallery bulk-write is axe-clean`, `Guestbook is axe-clean`, `Share is axe-clean`, `Settings is axe-clean`, `Album-leave prompt is axe-clean`, `Rotate management link confirmation is axe-clean`, `Rotate management link sensitive result is axe-clean`, `RSVP pending-work prompt is axe-clean`, `Settings pending-work prompt is axe-clean`, `Move to Recently deleted dialog is axe-clean`, `Entry rotation confirmation is axe-clean`, `Entry disable confirmation is axe-clean`, `paused guest main page is axe-clean`, `paused guest fullscreen is axe-clean`, `pending registration route is axe-clean`, `Host Events search and sort is axe-clean`, `Public Album nonempty is axe-clean`, `Public Album empty is axe-clean`, `Slice 5 named Axe inventories are exact and unique`) |
| **C-43** — the six Manager destination labels visually collided at 320px | `implemented` | At 320px the six source-ordered destinations render as two rows of three with nonintersecting at-least-14px labels, contained at-least-12px counts, and at-least-44px targets. Shared offsets also keep Manager navigation, Gallery controls, Undo, and selection tray disjoint, while the 320×568 policy constrains rather than hides full state and keeps every control reachable. | `tests/e2e/manager-responsive.spec.ts` (`320 Manager navigation labels do not intersect`, `the mobile Library tray, reopened Undo, Album, and Guest gallery stay reachable and contained`) |
| **C-44** — deep Library scroll was lost after visiting a shorter Gallery mode | `implemented` | Departing rendered Library, Album, or Guest-gallery rows are captured as a bounded ID anchor with viewport offset and fallback scroll. Restoration waits for the destination rows, tries the exact rendered ID and then bounded alternating nearby IDs, and otherwise clamps the saved scroll to current document bounds; a newer adoption or restoration generation cancels stale work. Restoration searches only already-rendered items and never fetches, loads more, or advances a cursor. | `tests/unit/manager-history-state.test.ts` (`bounds candidate IDs to 20 and alternates after then before`), `tests/ui/gallery-anchor.test.ts` (`captures the first rendered item crossing the effective visible top with bounded neighbors`, `restores exact and alternating rendered candidates to the saved viewport offset`, `clamps raw fallback scrolling when no saved candidate remains rendered`), `tests/ui/album-workspace.test.tsx` (`captures and restores the real requested Library, Album, and Guest-gallery roots`, `defers Library anchor restoration until its initial rows commit`, `defers Album anchor restoration until its initial rows commit`, `defers Guest-gallery anchor restoration until its current initial query settles`), `tests/ui/app.test.tsx` (`captures a Library anchor before Guest gallery and restores it after Back`, `waits for a delayed Library return before restoring a cross-section Back anchor`, `waits for a delayed Album return before restoring its Back anchor`, `clears a pending delayed restoration before a later ready signal`, `uses nearby rendered IDs then clamped scroll without fetching`, `captures Library and Guest-gallery anchors before Back or Forward adopts another location`, `cancels queued Gallery restoration after a newer adoption`), `tests/e2e/manager-navigation-intents.spec.ts` (`deep Library anchor survives Guest gallery and Back`) |
| **C-45** — purpose-bearing Manager routes lost their exact task, target focus, and return context | `implemented` | Manager owns exactly three one-use paths: Share → Complete export, a retained Album marker → Recently deleted, and Hidden Guest gallery → Settings → Hidden Guest gallery. Each intent is consumed and its canonical Router entry is cleaned before focus while unrelated Router state remains. Complete export focus advances from its labelled region only when the intended action becomes ready; the retained path selects Recently deleted and focuses either a first-page Restore control or the heading with bounded Load-more guidance, without fetching; and the Settings round trip focuses availability, clears and announces Guest-gallery selection, waits for existing save settlement, restores Hidden, and focuses Open settings only while Guest gallery remains off and that existing action remains present. Back, Forward, and reload do not replay the consumed tasks; unrelated navigation retires the Guest-gallery return. | `tests/unit/manager-history-state.test.ts` (`consumes a compatible intent while preserving foreign state and anchors`), `tests/unit/guestbook-export.test.ts` (`advances semantic complete export focus only after loading becomes ready`), `tests/ui/album-workspace.test.tsx` (`hands the retained marker intent and opaque media ID to the navigation owner`), `tests/ui/event-settings-editor.test.tsx` (`uses each positive availability focus epoch once, including epoch 1 on initial mount`), `tests/ui/host-private-gallery.test.tsx` (`Guest gallery Settings exposes every filter and All requests the unfiltered collection`, `Guest gallery Settings passes Hidden after clearing selection and announcing it`), `tests/ui/app.test.tsx` (`uses a Share complete export intent once, focuses its enabled action, and preserves foreign state`, `commits one canonical clean entry before a complete export intent focuses under StrictMode`, `uses a retained marker intent once, focuses its first-page Restore, and does not replay on Back or reload`, `clears settled retained guidance when the host exhausts Load more`, `keeps a preloaded page-2 retained target outside the bounded intent search`, `omits retained Load-more guidance after the host exhausts the continuation`, `Guest gallery Settings round trip cleans each intent, waits for save, and restores Hidden focus once`, `Guest gallery Settings drops its return token after unrelated navigation`), `tests/e2e/manager-navigation-intents.spec.ts` (`Share opens complete export`, `retained Album slot opens Recently deleted`, `Guest gallery Settings round trip restores Hidden and focus`) |
| **C-46** — Viewer Next disabled at the last loaded photo even when more pages exist | `implemented` | Identity-driven `GalleryViewer` delegates the owner-provided successor ID and fences stale results by request generation, current photo, and mount. `ManagerPrivateGallery` remains the sole cursor/page-append owner; its append is single-flight, filters duplicate rows, and advances only when the immediate successor exists. A recoverable append failure returns a dialog-local failure with focused `Try again`; clean exhaustion disables `Next photo` without an alert. Close and replacement-query retirement reject stale advanced/failed results while a successful append remains in the mosaic, and the original invoker remains the viewer's restoration target. A failed same-event replacement retires its prior cursor and continuation but preserves the confirmed rendered sequence for Previous/Next until an exact retry succeeds. | `tests/ui/gallery-viewer.test.tsx` (`requests the next page from the last loaded photo while keeping that photo visible`, `keeps one continuation in flight across repeated next activation`, `changes to the owner-provided next photo ID after continuation advances`, `renders an unavailable Next photo control when continuation is exhausted`, `offers a focused retry after continuation fails without changing the current photo`, `ignores a stale continuation after Previous changes the current photo`, `does not render a stale continuation failure after Previous changes the current photo`, `ignores a stale continuation after Close`, `unmounts after Close without a late failed continuation reopening dialog-local error UI`), `tests/ui/host-private-gallery.test.tsx` (`loads one next page in the viewer, deduplicates it, and advances to the immediate successor`, `restores focus to the original tile after viewer continuation advances`, `keeps the viewer photo and focuses Try again after a continuation failure`, `marks viewer continuation exhausted without an alert when no successor remains`, `coalesces click and ArrowRight while viewer continuation is pending`, `retains an appended page without reopening a viewer closed during continuation`, `retires a stale viewer continuation before a replacement uses its own cursor`, `keeps retained viewer navigation usable after a failed replacement and retries that exact search`), `tests/e2e/accessibility.spec.ts` (`viewer crosses a Gallery page boundary without losing its failed continuation state`) |
| **C-63** — secret-fragment cleanup rewrote every public Album URL to `/album` and dropped legitimate query parameters, including cookie-only loads | `implemented` | Public Album cleanup calls `replaceState` only for a present token fragment, preserves the current pathname and query, and leaves cookie-only URLs untouched while reusing the narrow Album-share cookie. | `tests/ui/album-share-page.test.tsx` (`erases the fragment before exchanging it and never renders the credential`, `reuses the narrow cookie on reload and renders only narrow preview URLs`) |
| **C-64** — a failed public cover retained failure state when `coverMediaId` changed | `implemented` | Public Album cover failure state is owned by an image component keyed by `coverMediaId`, so a changed cover ID remounts with fresh image state and the new preview can load. | `tests/ui/album-share-page.test.tsx` (`resets a failed cover when the album chooses a different cover photo`) |

## Slice 5 — Lifecycle and contribution

**Progress only — no finding disposition:** Migration `0021_manager_upload_and_album_era.sql`
and its populated-0020 compatibility contract are covered by
`tests/worker/migration-0021.test.ts`. The server-only account upload actor and the four Manager
upload routes are covered by `tests/worker/manager-upload-actor.test.ts` and
`tests/worker/manager-upload-api.test.ts`. The service- and SQL-layer intake split keeps the guest
pause and schedule on guest authority while allowing live Manager authority through the same upload
pipeline; `tests/worker/repositories.test.ts`, `tests/worker/upload-api.test.ts`, and
`tests/worker/photo-intake-api.test.ts` own that server evidence. The atomic, revision-CAS rotation
transaction that C-10 depends on is covered by `tests/worker/manage-api.test.ts` and
`tests/worker/manager-upload-actor.test.ts`. This is server progress only; no Manager-dialog or guest
surface behavior is claimed here.

**Safety-ladder precursor evidence:** Nine existing actions now carry their
consequence-based request timing. Pick / unpick, Publish / hide, and Remove from Album with Undo are
immediate and dialog-free (`tests/ui/album-workspace.test.tsx`). Stop the Album link, Rotate the
Manager link, and Move an original to Recently deleted send nothing until their focused explicit
confirmations succeed (`tests/ui/album-workspace.test.tsx`, `tests/ui/app.test.tsx`, and
`tests/ui/manager-recovery.test.tsx`). Disable the printed entry, Sign out all guest devices, and
Delete event send nothing until the exact event name matches and the explicit action is activated
(`tests/ui/app.test.tsx`). The C-16 row below joins those nine rungs with the renamed immediate
**Pause / Resume guest uploads** tenth rung; the precursor paragraph is not a separate disposition.

| Finding | Disposition | What changed | Owning tests |
| --- | --- | --- | --- |
| **C-08** — Pause photo delivery removes the shared gallery, Guestbook, and My deliveries on the main guest page, while fullscreen follows a different rule | `implemented` | Pause now gates only new guest uploads. The server-selected `UploadAuthority` keeps guest pause/schedule predicates separate from Manager intake, and Intake keeps one availability-resolved **Add photos** trigger even while guest uploads are paused. A separate server-owned `guestReadSurfaces` projection gates direct Gallery, My deliveries, and Guestbook reads before photo sharing opens, then preserves all three after that boundary regardless of pause; Guest-gallery visibility remains independently owned. The main route and fullscreen use the same Gallery availability and ordered item projection. Fullscreen parity is deliberately Gallery-only: its compact route does not duplicate Guestbook or My deliveries panels from the main page. | `tests/worker/repositories.test.ts`, `tests/worker/upload-api.test.ts`, `tests/worker/manager-upload-api.test.ts`, `tests/worker/photo-intake-api.test.ts`, `tests/worker/core-journey.test.ts`, `tests/worker/messages-api.test.ts`, `tests/ui/manager-upload-dialog.test.tsx`, `tests/ui/app.test.tsx`, `tests/e2e/guest-lifecycle.spec.ts`, `tests/e2e/accessibility.spec.ts`, `tests/e2e/guest-responsive.spec.ts` |
| **C-09** — registration says confirmation can happen later, but the account does not exist until the code is entered; sign-in then reports a credential error | `implemented` | Registration now says when the account exists, returns the exact pending expiry, and persists only a versioned email digest plus expiry. The browser-cookie-only pending status is email-blind and private/no-store; matching sign-in resumes confirmation before any password request, while false, expired, completed, restart, and Start-over paths clear the marker. Delivered resend refreshes only the guarded pending snapshot. | `tests/worker/host-auth.test.ts` (`returns the exact resumeExpiresAt while creating no account before mailbox proof`, `answers register/pending only from the browser registration cookie and never an email`, `lets only one concurrently delivered resend replace the shared pending snapshot`), `tests/worker/host-auth-boundary.test.ts` (`keeps register/pending read-only, email-blind, and outside mutation rate limits`), `tests/unit/pending-registration.test.ts` (`round-trips only the normalized email digest and exact expiry`), `tests/ui/host-auth.test.tsx` (`stores none of the five forbidden secrets through full registration and sign-in submits`, `intercepts a matching password submit across mounts, reloads pending, and clears on Start over`), `tests/e2e/accessibility.spec.ts` (`pending registration route is axe-clean`) |
| **C-10** — Manager-link rotation is offered where ownership makes it impossible, the error repeats below the fold, and the successful owned-event path discards the replacement link | `implemented` | The Manager projection enables rotation only for current account owner/cohost authority and exposes a revision, never token IDs; link-only use stays focusably disabled with its account path and a direct probe returns `ROLE_FORBIDDEN`. The revision-CAS transaction admits one winner, revokes the exact predecessor, preserves account actors, and cancels link-owned work. Confirmation defaults to Keep current link; success shows the masked replacement and blocks Escape, Back, programmatic navigation, and reload until Copy or explicit fallback acknowledgement, without navigating through the bearer link. | `tests/worker/manage-api.test.ts` (`rotate projects the client revision only through account authority and validates bodies strictly`, `rotate admits one concurrent revision winner and a delayed stale request cannot replace it`, `rotate preserves the owner-claim precondition and refuses link-only access`), `tests/worker/manager-upload-actor.test.ts` (`evaluates the guarded insert after rotation and binds only the replacement token`), `tests/ui/app.test.tsx` (`holds the rotate result behind copy, navigation, reload, and retired-resource gates`, `requires explicit rotate fallback acknowledgement before continuing`), `tests/ui/copyable-link-card.test.tsx` (`reports the settled sensitive copy outcome to a rotation save gate`), `tests/e2e/accessibility.spec.ts` (`Rotate management link confirmation is axe-clean`, `Rotate management link sensitive result is axe-clean`), `tests/e2e/manager-navigation-intents.spec.ts` (`rotation save gate refuses Back and reload until Copy releases account-session navigation`) |
| **C-12** — a host cannot upload a photo to their own event anywhere in Manager | `implemented` | Four Manager upload routes reuse the one reserve/transfer/finalize/cancel pipeline with server-selected link or account authority and fixed `Host` attribution. The Manager dialog adds the queue's delivered media ID, quiet canceled terminal, typed failure, and one `onFinalized` transition; its cleanup owner covers lost reserve, cancel, finalize, terminal, and late-result races while the always-present Intake trigger owns focus, navigation blocking, and targeted invalidation. Terminal observability is end-to-end: the transport preserves a typed content-response failure, the queue retains it in `failure`, and the dialog's real flat 403 case drives terminal handoff. The Worker projection emits only `media-cap` and `storage-cap` reasons (or enabled/null); `event-unavailable` is the fourth client-derived member from `resolveHostUploadAvailability`, with canonical expired-event behavior proved by a 410 response rather than an invented Worker projection row. | `tests/worker/manager-upload-actor.test.ts`, `tests/worker/manager-upload-api.test.ts`, `tests/worker/repositories.test.ts`, `tests/worker/upload-api.test.ts`, `tests/unit/upload-queue.test.ts`, `tests/unit/upload-flow-ownership.test.ts`, `tests/unit/manager-upload-cleanup.test.ts`, `tests/unit/browser-upload-transport.test.ts`, `tests/unit/host-upload-availability.test.ts`, `tests/ui/manager-upload-dialog.test.tsx`, `tests/ui/app.test.tsx`, `tests/e2e/accessibility.spec.ts`, `tests/e2e/manager-responsive.spec.ts` |
| **C-16** — destructive actions use unrelated confirmation patterns, with more friction on reversible actions than permanent ones | `implemented` | One consequence-based safety ladder now names all ten actions and pins their request timing. Pick / unpick, Publish / hide, Remove from Album with Undo, and **Pause / Resume guest uploads** are immediate, dialog-free reversible actions; Stop Album link, Rotate Manager link, and Move to Recently deleted wait for focused explicit confirmation; Disable printed entry, Sign out guest devices, and Delete event require the exact event name before the request. The first nine rungs retain their owning timing regressions, while the renamed Pause / Resume controls supply and assert the tenth without a confirmation or typed field. | `tests/ui/album-workspace.test.tsx`, `tests/ui/app.test.tsx`, `tests/ui/manager-recovery.test.tsx`, `tests/ui/manager-photo-intake.test.tsx`, `tests/e2e/accessibility.spec.ts` |
| **C-17** — a new Album with picks made moments earlier tells a false “before albums” story and forces a legacy reconciliation choice | `implemented` | Migration `0021_manager_upload_and_album_era.sql` installs the durable `media.album_pick_version` / event-owned `events.album_pick_generation` pair. The Worker projects only `pickGeneration` and exactly four reconciliation states: `null`, `initialize`, `historical`, or `over-capacity`. Every new-client Start carries the observed reconciliation, generation, and revision into one expectation-guarded CAS; each successful new-client choice advances revision once, and a PUT using the pre-Start revision is rejected without replacing the started entries. The editor renders those server-owned states, sends one observation, adopts the advanced revision, and keeps the existing request/queue owner. Its Manager-only retained marker now carries ordering-only `timelineAt`, so the real Reset action interleaves active and retained picks in timeline order without changing the public projection. | `tests/worker/migration-0021.test.ts` (`backfills only picked rows belonging to saved Albums`, `commits the predecessor-shaped pick and stamps version 1`, `increments independently and exactly once for every Album eligibility transition`), `tests/worker/album-api.test.ts` (`projects reconciliation for $name`, `keeps one coherent reconciliation observation across a concurrent unpick`, `requires all three new-client expectations rather than widening the legacy shape`, `a matching %s Start advances revision once, blocks the pre-Start PUT, then an ordinary save advances it once more`, `conflicts on a same-count substitution even though the category and count are unchanged`), `tests/ui/album-workspace.test.tsx` (`uses only Album-pick terminology in the exact one-time reconciliation state`, `reconciliation auto-starts one StrictMode observation once and adopts the advanced revision`, `reconciliation manual %s sends the complete observed expectation triple`, `reconciliation obeys an over-capacity projection with zero historical picks`, `reconciliation null with zero picks renders the ordinary empty Album`, `orders live and retained slots together during reconciliation-era Reset with truthful copy`) |
| **C-49** — at 501 historical picks, the Worker rejects Start empty before it can clear picks | `verified-existing` | The existing `AlbumRepository.start` repair keeps `empty` unconditional with respect to the 500-entry cap; only `from-picks` is refused. This checkpoint adds the direct boundary regression: 500 active current-era picks plus one retained historical pick still let expectation-guarded Start empty save an empty Album and clear all 501 active/retained favorites. | `tests/worker/album-api.test.ts` (`lets Start empty clear and save an unsaved legacy set above the album cap`, `lets Start empty at 501 clear every active and retained favorite`), `tests/ui/album-workspace.test.tsx` (`reconciliation obeys an over-capacity projection with zero historical picks`) |
| **C-50** — empty Intake says No matching photos when no filter exists | `implemented` | `ManagerPage.renderMediaGrid` now distinguishes true empty (`media.length === 0`, all statuses, no contributor filter) from a filtered empty result. True empty keeps the printable QR, says **No photos yet** and **Guests' photos arrive privately here.**, and reuses the existing Share and Add-photos owners; filtered empty keeps **No matching photos** and clears the active filter before reloading. Recently deleted and `ManagerPrivateGallery`/Library retain their own empty-state contracts unchanged. | `tests/ui/app.test.tsx` (`Intake true empty keeps the printable QR and opens the existing Share surface`, `Intake true empty returns Add photos focus to the actual toolbar or secondary invoker`, `Intake filtered empty clears the contributor filter and reloads an unfiltered first page`), `tests/ui/host-private-gallery.test.tsx` (`uses Delivered photos and In Album vocabulary for empty Library states`, `does not describe an unfiltered empty shared list as unpublished`), `tests/e2e/accessibility.spec.ts` (`true-empty Intake is axe-clean`) |
| **C-52** — the events dashboard has no create, search, sort, or archive affordance | `implemented` | Host Events adds a primary keyboard-reachable Create event link, case-insensitive local name search with a live count, and deterministic newest/oldest date sorting with loaded order as the equal-date tiebreak. Cards and ownership stay unchanged; Archive remains a later product decision. Event dates use the date-only formatter and management expiry uses each event's allowlisted zone. | `tests/worker/host-auth.test.ts` (`returns exactly the eventTimezone allowlist for each host event`), `tests/ui/host-events.test.tsx` (`searches loaded names case-insensitively and announces the local result count`, `round-trips newest and oldest while preserving loaded order for equal dates`, `makes Create event a primary keyboard-reachable link`), `tests/e2e/accessibility.spec.ts` (`Host Events search and sort is axe-clean`) |
| **C-53** — Album title defaults to literal Album and becomes invalid if cleared | `implemented` | A new Album projects and persists the event name as its first-run title, including expectation-guarded and compatibility Start paths, while preserving an existing customized title. The editor uses the event name as its placeholder, but a cleared field remains truly empty, invalid, and unsaved; the placeholder never becomes an autosave value. | `tests/worker/album-api.test.ts` (`uses the event name as a new Album title and preserves a customized title`), `tests/ui/album-workspace.test.tsx` (`uses the event name as the first-run Album title and keeps a cleared title invalid and unsaved`, `keeps an existing customized Album title while retaining the event-name placeholder`) |
| **C-55** — Guestbook's default tab changes with counts | `implemented` | Manager Guestbook now starts in **Needs review** for zero, one, or many pending items. Summary-count refreshes do not change the current view, and an explicit host selection such as Shared persists across the refresh. | `tests/ui/manager-guestbook.test.tsx` (`uses Needs review as the default view with %i pending items`, `keeps the current default view across count refreshes and preserves an explicit view`) |
| **C-56** — equal upload endpoints render as a duplicated time range | `implemented` | The independent Gallery timeline formatter collapses a real two-photo, seconds-apart group to one time when both endpoints format to the same event-zone minute. Different minutes, meridiems, one-photo groups, and cross-midnight groups retain the existing paths; this change does not import or widen the C-61 formatter. | `tests/unit/gallery-timeline.test.ts` (`collapses a multi-photo same-minute range`) |
| **C-57** — a large Cover upload has no progress or cancel state | `implemented` | The existing Cover draft session owns one attempt controller across reservation, raw XHR transfer, inspection, and natural preview. It publishes monotonic determinate byte progress with throttled polite announcements and fences late results. Cancel retires the generation, aborts and awaits the attempt, replays only an ambiguous reservation once with the same intent, rereads authoritative drafts, then enters the existing serialized discard reconciliation and restores picker focus; publication ownership remains unchanged. | `tests/ui/cover-studio-session.test.tsx` (`cover draft cancel ordering`), `tests/ui/cover-studio.test.tsx` (`cover upload progress`), `tests/e2e/accessibility.spec.ts` (`Cover upload progress is axe-clean`) |
| **C-58** — Pause pairs with Reopen rather than Resume | `implemented` | All four Manager intake states and their chip/status copy now name **Guest uploads**. The reversible controls are exactly **Pause guest uploads** and **Resume guest uploads**, keep one immediate request with no confirmation, and remain keyboard reachable and at least 44px at the narrow phone widths. | `tests/ui/manager-photo-intake.test.tsx` (`renders Guest uploads terminology in the Manager state chip for all four states`), `tests/ui/app.test.tsx` (`keeps Pause / Resume guest uploads reversible with one immediate request and no confirmation`), `tests/e2e/accessibility.spec.ts` (`Pause and Resume guest uploads stay keyboard reachable and touch-sized at 390px and 320px`) |
| **C-59** — registration confirmation ends without a route to the events dashboard | `implemented` | Confirmation has two terminal destinations: a still-valid bound-event context resumes its exact canonical Manager destination; standalone, invalid, mismatched, unbound, or failed-bind contexts go to Host Events. Failed binding truthfully says the account exists but the event was not saved, offers one Continue to Host Events action, clears pending state, and never submits confirmation twice. | `tests/unit/recovery.test.ts` (`resolves registration confirmation for %s`), `tests/ui/app.test.tsx` (`resumes the exact canonical destination for a bound event after registration confirmation`, `sends a standalone pending registration to Host Events after registration confirmation`, `keeps a truthful failed-bind result with one Host Events continuation after registration confirmation`) |
| **C-61** — retention and event dates use two formatters/locales | `implemented` | `src/app/event-date-time.ts` is the one canonical event-zone formatter owner for exactly four surfaces: Intake schedule, Manager header/retention, upload flow, and Host Events. A targeted AST audit rejects date-valued local `Intl.DateTimeFormat` and locale calls in those four files while explicitly whitelisting only named numeric `toLocaleString` count receivers. `validInstant` requires an explicit offset and a calendar-valid date, failing closed for bare, zoneless, impossible, and non-leap values; equal formatted range endpoints collapse through the existing shared range formatter. Gallery timeline and other ruled-out surfaces remain outside C-61. | `tests/unit/event-date-time.test.ts`, `tests/unit/slice5-date-formatting-ast.test.ts`, `tests/ui/app.test.tsx` (`uses the event zone formatter for the Manager header, retention, and Intake schedule`, `renders unavailable literals for invalid event zone values instead of normalizing them`), `tests/ui/host-events.test.tsx` (`uses the date-only formatter and each event zone for its management-expiry instant`) |

## Slice 6

Not started. It fills its own section on the same terms.
